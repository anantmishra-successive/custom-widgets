/**
 * Provides an access to a private function, considering its source patch and name.
 * This is possible thanks to the plugin `babel-plugin-rewire-ts`.
 * See more info: https://github.com/rosswarren/babel-plugin-rewire-ts.
 * @param privateFunctionFolder The folder under src containing the private function
 * @param functionName The function's name
 */
export declare function getPrivateFunction(functionFolder: string, functionName: string): any;
/**
 * Provides a way to mock a function from the src folder.
 * @param functionFolder The folder under src containing the private function
 * @param functionName The function to mock
 * @param mockedImplementation The mocked implementation
 */
export declare function mockFunction(functionFolder: string, functionName: string, mockedImplementation: any): void;
/**
 * Resets the mocked implementation of a specific private function
 * @param functionFolder The folder under src containing the private function
 * @param functionName The function to mock
 */
export declare function resetMockFunction(functionFolder: string, functionName: string): void;
