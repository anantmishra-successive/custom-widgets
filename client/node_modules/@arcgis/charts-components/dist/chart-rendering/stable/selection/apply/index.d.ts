import { SerialChart, PieChart, XYChart } from '@amcharts/amcharts4/charts.js';
import { WebChart, WebChartDataTypes, WebGaugeChart } from '@arcgis/charts-spec';
import { ErrorStrings, QueryObject, SelectionSource } from '../../../../utils';
import { ChartOptions } from '../../../common/interfaces';
import { ElementSelectionProps } from '../interface';
import { ApplySelectionOnSeriesProps } from './interfaces';
/**
 * Apply a selection on a specific series
 *
 * @param props
 */
export declare function applySelectionOnSeries(props: ApplySelectionOnSeriesProps): void;
/**
 * Apply a selection on a chart
 *
 * @param props
 */
export declare function applySelectionOnChart(props: {
    chart: SerialChart;
    chartConfig: WebChart | WebGaugeChart;
    chartData?: WebChartDataTypes;
    queryObject: QueryObject;
    options?: ChartOptions;
    dataWasUpdated?: boolean;
    rotationChanged?: boolean;
    sortChanged?: boolean;
    groupOfSlicesWasUpdated?: boolean;
    errorStrings: ErrorStrings;
}): Promise<void>;
/**
 * Applies a selection to the chart, based on a list of Sprite elements and additional information
 *
 * @param props
 */
export declare function applySelection(props: {
    chart: PieChart | XYChart;
    listSelectedElements: ElementSelectionProps[];
    selectionSource: SelectionSource;
}): Promise<void>;
