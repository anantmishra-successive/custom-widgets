import { Series } from '@amcharts/amcharts5';
import { ListAutoDispose } from '@amcharts/amcharts5/.internal/core/util/List.js';
/**
 * Function swapping the series following the position changes array.
 * Ignores individually the swap request if the indexes are invalid.
 * @param series The series to swap
 * @param positionChanges The array of position changes to apply
 */
export declare function swapSeries(series: ListAutoDispose<Series>, positionChanges: number[][]): void;
