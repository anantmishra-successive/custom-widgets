{
  "name": "arcgis-experience-builder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build:prod",
    "start": "cd server && npm install && node ./src/server.js --http_only"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
