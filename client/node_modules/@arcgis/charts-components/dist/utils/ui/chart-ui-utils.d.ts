import { WebChart, WebChartTypes } from '@arcgis/charts-spec';
export declare function getChartIcon(chart: WebChart): string;
/**
 * Gets the chart type text based on the provided chart type.
 * @param chartType
 * @category Private
 */
export declare function getChartText(chartType: WebChartTypes): Promise<string>;
