import { WebChart, WebChartDataTransformations } from '@arcgis/charts-spec';
import { ChartOptions } from '../../chart-rendering/common/interfaces';
import { LoaderColors, SelectionData, SelectionTheme } from '../../utils';
import { CategoryNames, ControlLabels } from '../common';
interface NumberOptions {
    min?: number;
    max?: number;
    step?: number;
}
export interface BooleanCheckOptions {
    label: string;
}
export interface InlineRadioOptions {
    values: string[];
}
export interface SelectOptions {
    values: string[];
}
export interface ControlDefinition {
    category: CategoryNames;
    subcategory?: CategoryNames;
    label: ControlLabels;
    type: "boolean" | "check" | "inline-radio" | "number" | "object" | "range" | "select" | "text";
    options?: BooleanCheckOptions | InlineRadioOptions | NumberOptions | SelectOptions;
    defaultValue?: ChartOptions | LoaderColors | SelectionData | SelectionTheme | WebChart | WebChartDataTransformations | boolean | number | string | null;
}
export {};
