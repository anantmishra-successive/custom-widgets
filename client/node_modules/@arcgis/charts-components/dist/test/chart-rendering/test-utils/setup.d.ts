import { Root } from '@amcharts/amcharts5';
import { Axis, AxisRenderer, XYChart } from '@amcharts/amcharts5/xy.js';
type AxisType = "Category" | "Date" | "Value";
export declare function createXYChart(axisTypes: [AxisType, AxisType]): {
    root: Root;
    chart: XYChart;
};
export declare function getXAxis(chart: XYChart): Axis<AxisRenderer>;
export declare function getYAxis(chart: XYChart): Axis<AxisRenderer>;
/**
 * Need `SVGPathElement` on `window` to resolve the following error:
 * "ReferenceError: SVGPathElement is not defined"
 * SVGPathElement is `window` object's property on browser. It is NOT available on node, even with `jsdom`.
 * - `Jest` Issue: https://github.com/c3js/c3/issues/2129
 * - `jsdom` Issue: https://github.com/jsdom/jsdom/issues/2126
 */
export declare function defineSVGPathElement(): void;
export {};
