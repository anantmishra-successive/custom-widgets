import { Chart } from '@amcharts/amcharts5';
import { AxisRenderer, DateAxis } from '@amcharts/amcharts5/xy.js';
import { DateTimeFormatOptions } from '@arcgis/charts-spec';
/**
 * Returns true if the input chart is a XYChart and uses a date axis
 * @param chart The instance of chart to be tested
 */
export declare function isDateXAxis(chart: Chart): boolean;
/**
 * Sets the date axis format options:
 * - timezone (TODO: to be implemented)
 * - date formats and period change date formats
 *
 * @param axis The axis
 * @param formatOptions The format options
 */
export declare function setDateAxisFormatOptions(axis: DateAxis<AxisRenderer>, formatOptions: DateTimeFormatOptions): void;
