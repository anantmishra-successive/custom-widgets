import { WebChart } from '@arcgis/charts-spec';
import { ChartRendererType } from '../renderer/interfaces';
import { ConfigInfo } from './interfaces';
import { ArcgisChart } from '../../components/arcgis-chart/customElement.js';
export declare function geMultiChartsDivId(): string;
export declare function getChartRef(): string;
/**
 * Adds a data source and a renderer to a chart config
 *
 * @param props.chartConfig The chart configuration to update
 * @param props.iLayerKey The key of the iLayer to add
 * @param props.fileKey The key of the file containing the data sources
 * @param props.chartIndex The index of the chart in the file
 */
export declare function addILayer(props: {
    chartConfig: WebChart;
    iLayerKey?: string;
    fileKey: string;
    chartIndex: number;
}): void;
/**
 * Adds a chartRenderer to the chart config
 *
 * @param chartConfig The chart configuration to update
 * @param chartRendererType The type of renderer to add to the chart
 */
export declare function addChartRenderer(chartConfig: WebChart, chartRendererType?: string): void;
export declare function retrieveOriginalChartConfig(chartElement: HTMLArcgisChartElement | undefined): Promise<ConfigInfo | undefined>;
export declare function retrieveAllChartsConfigIndexes(chartDiv: HTMLElement | undefined): {
    fileIndex: number;
    chartIndex: number;
    chartRendererType?: ChartRendererType;
    version?: "v4" | "v5";
}[];
