/// <reference types="../../index.d.ts" />
import { JsxNode, PublicLitElement as LitElement, TargetedEvent } from '@arcgis/lumina';
import { MeasurementState, MeasurementValue } from '../../support/measurementUtils';

/**
 * Component which displays the contents of a measurement content.
 *
 * @private
 */
export declare class ArcgisMeasurementContent extends LitElement {
    hintLabel?: string;
    items: {
        title?: string;
        measurement: MeasurementValue;
    }[];
    /** @default "disabled" */
    state: MeasurementState;
    /** @default "" */
    unsupportedLabel: string;
}
