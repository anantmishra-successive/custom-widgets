import { ColumnSeries } from '@amcharts/amcharts5/xy.js';
import { WebChartHistogramSeries } from '@arcgis/charts-spec';
import { HistogramLabelFormatCallback } from '../../../../common/interfaces';
import { TooltipProps } from '../interfaces';
export type HistogramTooltipProps = TooltipProps<ColumnSeries, WebChartHistogramSeries, HistogramLabelFormatCallback>;
export interface HistogramDataContext {
    count: number;
    minValue: number;
    maxValue: number;
}
