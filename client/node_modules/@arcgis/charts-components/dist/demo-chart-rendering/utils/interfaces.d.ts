import { IFeatureLayer, WebChart } from '@arcgis/charts-spec';
export interface ConfigInfo {
    chartConfig: WebChart;
    fileId: number;
    chartIndex: number;
}
/**
 * Defines the best grid size according to the number of charts.
 * For instance `8: [3, 3]` reads 3 cols or 3 columns when there are 8 charts.
 */
export declare const mapBestGridSize: Record<number, [number, number]>;
/** JSON representation of the feature layer data source */
export interface FormerWebChartLayerDataSource {
    /** Represents a unique string identifier for the type of chart data source. */
    type: "layer";
    /** Carries the properties necessary to build a JSAPI Feature Layer in order to fetch the chart data. */
    layer: IFeatureLayer;
}
