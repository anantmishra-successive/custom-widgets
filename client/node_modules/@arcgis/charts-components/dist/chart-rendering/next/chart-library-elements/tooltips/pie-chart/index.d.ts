import { Tooltip } from '@amcharts/amcharts5';
import { PieSeries } from '@amcharts/amcharts5/percent.js';
import { WebChartPieChartSeries } from '@arcgis/charts-spec';
import { PieChartLabelFormatCallback } from '../../../../common/interfaces';
import { PieChartTooltipProps } from './interfaces';
export declare function generateDefaultPieChartTooltipContentFormatter(pieSeries: PieSeries, seriesConfig: WebChartPieChartSeries): PieChartLabelFormatCallback;
/**
 * Adds the html code to the tooltip associated to the pie series slices
 * @param seriesConfig The series config
 * @param pieSeries the pie series parent of the element
 * @param tooltip the tooltip that we are adding editing
 */
export declare function pieChartTooltipContentCallback(props: {
    seriesConfig: WebChartPieChartSeries;
    pieSeries: PieSeries;
    tooltip: Tooltip;
}): string | undefined;
/**
 * Initializes the tooltip associated to the slices on a pie chart
 * @param pieSeries The series containing the slices on which to add a tooltip text adapter
 */
export declare function initializePieChartTooltip(pieSeries: PieSeries): void;
/**
 * Handles how the tooltip is rendered on a pie chart.
 * Uses from the configuration:
 *   - the numeric value format defined at the series level
 *   - the percentage value format defined at the series level
 * If a format callback is passed, we'll use it over the default tooltip formatting.
 * @param props Necessary information to update the tooltip
 */
export declare function updatePieChartTooltip(props: PieChartTooltipProps): void;
