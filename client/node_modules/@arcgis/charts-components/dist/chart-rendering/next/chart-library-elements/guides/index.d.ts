import { Axis, AxisRenderer } from '@amcharts/amcharts5/xy.js';
import { CommonStrings } from '../../../../utils';
import { WebChartGuide } from '@arcgis/charts-spec';
import { GuideTooltipFormatCallback } from '../../../common/interfaces';
/**
 * Retrieve the start and end values of the guide from the config. The process also tries to convert
 * the value accordingly (depending on whether it's a Date, Percentage, decimal, etc).
 *
 * @param axis The axis to which the guide attaches.
 * @param guideConfig The guide's config
 */
export declare function getGuideValuesFromConfig<T extends Axis<AxisRenderer>>(axis: T, guideConfig?: WebChartGuide): {
    isValidGuide: boolean;
    start?: Date | number | string | null;
    end?: Date | number | string | null;
};
/**
 *
 * @param props.axis The axis to update
 * @param props.anyGuideAboveChanged Whether any guide's above property was changed
 * @param props.guideConfigs The guides config
 * @param props.guideTooltipFormatter The guide tooltip formatter
 * @param props.commonStrings The t9n common strings
 */
export declare function updateAxisGuides<T extends Axis<AxisRenderer>>(props: {
    axis: T;
    anyGuideAboveChanged?: boolean;
    guideConfigs?: WebChartGuide[];
    guideTooltipFormatter?: GuideTooltipFormatCallback;
    commonStrings: CommonStrings;
}): void;
