import { XYChart } from '@amcharts/amcharts5/xy.js';
import { WebChart, WebChartDataTypes } from '@arcgis/charts-spec';
import { CommonStrings, ErrorStrings } from '../../../../../utils';
import { AxisLabelFormatCallback, GuideTooltipFormatCallback } from '../../../../common/interfaces';
/**
 * Update the axes for an XYChart
 *
 * @param props.chart The chart
 * @param props.chartConfig The chart's config
 * @param props.axisLabelsBehaviorChanged Whether the axis labels behavior changed
 * @param props.data The data to feed the axis
 * @param props.yAxisLabelFormatter The y-axis label formatter
 * @param props.guideTooltipFormatter The guide's tooltip formatter
 * @param props.commonStrings List of t9n strings
 * @param props.errorStrings The t9n error strings
 */
export declare function updateHistogramAxes(props: {
    chart: XYChart;
    chartConfig: WebChart;
    axisLabelsBehaviorChanged?: [boolean, boolean];
    data?: WebChartDataTypes;
    yAxisLabelFormatter?: AxisLabelFormatCallback;
    guideTooltipFormatter?: GuideTooltipFormatCallback;
    commonStrings: CommonStrings;
    errorStrings: ErrorStrings;
}): Promise<void>;
/**
 * Initializes the axes for an XYChart
 *
 * @param props.chart The chart
 * @param props.x The x field
 * @param props.axesConfig The axes config
 * @param props.errorStrings The t9n error strings
 */
export declare function initializeHistogramAxes(props: {
    chart: XYChart;
    x: string;
    axesConfig: WebChart["axes"];
    errorStrings: ErrorStrings;
}): Promise<void>;
