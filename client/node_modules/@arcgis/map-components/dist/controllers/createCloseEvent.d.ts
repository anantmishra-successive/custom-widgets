import { EventEmitter } from '@arcgis/lumina';
export declare const createCloseEvent: (component: {
    arcgisClose: EventEmitter;
    autoDestroyDisabled: boolean;
    destroy: () => void;
} & import('@arcgis/lumina').LitElement) => EventEmitter;
