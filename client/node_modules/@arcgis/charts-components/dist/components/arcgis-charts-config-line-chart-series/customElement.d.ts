/// <reference types="../../index.d.ts" />
import { LineChartModel } from '../../model';
import { JsxNode, PublicLitElement as LitElement, TargetedEvent } from '@arcgis/lumina';
import { PropertyValues } from 'lit';
import { PopoverOpenChangePayload } from '../../utils/components/event-payloads';
import { ArcgisChartsConfigLineChartSeriesPopover } from '../arcgis-charts-config-line-chart-series-popover/customElement.js';

/** @private */
export declare class ArcgisChartsConfigLineChartSeries extends LitElement {
    /** Overwrite localized strings for this component */
    messageOverrides?: typeof this._messages._overrides;
    /**
     * Line Chart Model.
     *
     * @category Public
     */
    model: LineChartModel;
    /**
     * To open the config of the selected series.
     *
     * @category Public
     * @default false
     */
    configSeriesOpen: boolean;
    /**
     * Whether the chart is actively using color match at the moment.
     *
     * @category Private
     * @default false
     */
    activeColorMatch: boolean;
    /**
     * place the popover in this placement.
     *
     * @category Public
     * @default "leading"
     */
    popoverPlacement: HTMLCalcitePopoverElement["placement"];
    /**
     * Event when there is change in series popover.
     *
     * @category Public
     */
    readonly arcgisChartsConfigLineChartSeriesPopoverChange: TargetedEvent<this, {
        open: boolean;
        element: ArcgisChartsConfigLineChartSeriesPopover["el"] | null;
    }>;
    /**
     * Event emitted when the popover is closed.
     *
     * @category Public
     */
    readonly arcgisChartsConfigPopoverOpenChange: TargetedEvent<this, PopoverOpenChangePayload>;
    private _messages;
}
