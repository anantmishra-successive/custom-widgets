import { WebChart, WebGaugeChart } from '@arcgis/charts-spec';
import { ControlDefinition } from './interfaces';
export declare function addStackedTypeControls(chartConfig: WebChart): ControlDefinition[];
export declare function getCommonControls(props: {
    chartConfig: WebChart | WebGaugeChart;
    configFileKey: string;
    allCharts?: boolean;
}): ControlDefinition[];
