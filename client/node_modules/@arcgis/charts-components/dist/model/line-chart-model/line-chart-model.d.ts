import { ChartConfig } from '../../utils';
import { WebChart, WebChartLineChartSeries } from '@arcgis/charts-spec';
import { ModelParams } from '../interfaces/chart-interfaces';
import { LayerInfo, ModelTypes } from '../interfaces/common';
import { ModelWithBaselineStacking, ModelWithLineMarkerStyling } from '../mixins';
import { SerialChartModel } from '../serial-chart-model/serial-chart-model';
export declare class LineChartModel extends SerialChartModel {
    /**
     * Common strings used in the model.
     * @category Private
     */
    private commonStrings;
    /**
     * Current configuration used for the line chart.
     */
    protected _config: ChartConfig<typeof ModelTypes.LineChart, WebChart>;
    /**
     * Default series for the line chart. Used in resetting the series styling.
     * @category Protected
     */
    protected defaultSeries: WebChartLineChartSeries | undefined;
    protected _chartType: "lineSeries";
    protected layerInfo: LayerInfo;
    setup(params: ModelParams<ChartConfig<typeof ModelTypes.LineChart>>): Promise<void>;
    protected generateDefaultConfig(): Promise<Omit<typeof this._config, "iLayer">>;
    /**
     * Initializes the default series for the line chart. Used in resetting the series styling.
     * @category Private
     */
    protected initDefaultSeries(): Promise<void>;
    /**
     * Resets the series styling to the default styling.
     */
    resetSeriesStyling(): void;
    /**
     * Returns whether the series styling is able to be reset.
     * @returns {boolean}
     */
    resetAvailable(): boolean;
}
export interface LineChartModel extends ModelWithBaselineStacking, ModelWithLineMarkerStyling {
}
