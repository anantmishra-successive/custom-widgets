import { SerialChart, Series } from '@amcharts/amcharts4/charts.js';
import { ListTemplate } from '@amcharts/amcharts4/core.js';
import { ChartOptions } from '../../../common/interfaces';
import { LoaderElements, LoaderHandle } from './interfaces';
/** Initialize the loader and defines the nested functions
 *
 * @param chart The chart to which the loader is attached
 * @param options The chart options
 */
export declare function initializeLoader(chart: SerialChart, options?: ChartOptions): {
    loaderElements: LoaderElements;
    loaderHandle: LoaderHandle;
};
/** Updates the loader and spinner colors
 *
 * @param loaderElements The loader elements
 * @param updateOptions Update options
 */
export declare function updateLoaderAndSpinnerColors(loaderElements: LoaderElements, updateOptions?: ChartOptions): void;
/**
 * Checks whether all the non-overlay series are `"ready"`
 *
 * @param series The series to check
 */
export declare function whetherSeriesReady(series: ListTemplate<Series>): boolean;
/**
 * Attaches a hide-loader handle to the input series's `"ready"` event
 *
 * @param seriesInstance The series instance to attach the loader handle to
 * @param hideLoaderHandle The handle to hide the loader
 */
export declare function attachLoaderHandle(seriesInstance: Series, hideLoaderHandle: LoaderHandle["hideLoader"]): void;
