import { ChartConfig } from '../../../utils';
import { ISimpleLineSymbol, WebChartCursorCrosshair } from '@arcgis/charts-spec';
/**
 * Gets the visibility of the horizontal line of the cursor's crosshair from the chart config.
 * @param config The chart configuration
 * @returns {boolean}
 * @remarks if the cursor crosshair or its horizontal line visibility is not set, the default value of `false` will be returned.
 */
export declare const getCursorCrosshairHorizontalLineVisibility: (config: ChartConfig) => boolean;
/**
 * Sets the visibility of the horizontal line of the cursor's crosshair in the chart config.
 * @param visibility The visibility to set for the cursor's crosshair horizontal line
 * @param config The chart configuration
 */
export declare const setCursorCrosshairHorizontalLineVisibility: (visibility: boolean, config: ChartConfig) => void;
/**
 * Gets the visibility of the vertical line of the cursor's crosshair from the chart config.
 * @param config The chart configuration
 * @returns {boolean}
 * @remarks if the cursor crosshair or its vertical line visibility is not set, the default value of `false` will be returned.
 */
export declare const getCursorCrosshairVerticalLineVisibility: (config: ChartConfig) => boolean;
/**
 * Sets the visibility of the vertical line of the cursor's crosshair in the chart config.
 * @param visibility The visibility to set for the cursor's crosshair vertical line
 * @param config The chart configuration
 */
export declare const setCursorCrosshairVerticalLineVisibility: (visibility: boolean, config: ChartConfig) => void;
/**
 * Gets the style of the cursor's crosshair from the chart config.
 * @param config The chart configuration
 * @returns {boolean}
 */
export declare const getCursorCrosshairStyle: (config: ChartConfig) => ISimpleLineSymbol | undefined;
/**
 * Sets the style of the cursor's crosshair in the chart config.
 * @param style The style to set for the cursor's crosshair
 * @param config The chart configuration
 */
export declare const setCursorCrosshairStyle: (style: ISimpleLineSymbol, config: ChartConfig) => void;
/**
 * Gets the cursor crosshair from the chart config.
 * @param config The chart configuration object
 * @returns {WebChartCursorCrosshair | undefined}
 */
export declare const getCursorCrosshair: (config: ChartConfig) => WebChartCursorCrosshair | undefined;
/**
 * Sets the cursor crosshair in the chart config.
 * @param cursorCrosshair The cursor crosshair to set
 * @param config The chart configuration object
 */
export declare const setCursorCrosshair: (cursorCrosshair: WebChartCursorCrosshair, config: ChartConfig) => void;
