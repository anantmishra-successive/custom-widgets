import { ChartConfig, ChartTypes } from '../../utils';
import { ModelOfType, ModelTypes } from '../interfaces/common';
import { ChartModel } from '../chart-model/chart-model';
/**
 * Converts a chart type used by charts-spec, to its corresponding model type used by the chart model.
 * @param chartType The chart type to convert
 * @returns The corresponding model type
 */
export declare function chartTypeToModelType<T extends ChartTypes>(chartType: T): ModelTypes;
export declare function chartTypeToModelType<T extends ChartTypes>(chartType: undefined): undefined;
export declare function chartTypeToModelType<T extends ChartTypes>(chartType: T | undefined): ModelTypes | undefined;
/**
 * Get the model type from the chart config
 * @param config The chart config
 * @returns The model type
 */
export declare function getModelTypeFromConfig<T extends ModelTypes>(config: ChartConfig<T>): T;
/**
 * Determines if the provided model's type matches the `type` argument.
 * @param model The model to check.
 */
export declare function isModelOfType<T extends ModelTypes | undefined>(model: ChartModel | undefined, type: T): model is ModelOfType<T>;
